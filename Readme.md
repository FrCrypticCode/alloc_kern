Memory Allocator Library in no_std
-- WORK IN PROGRESS --
Virtual Memory need to be implement.

Actually the library provide Allocator which has bytes, bitmap and an offset.

It's a component to be implement.